---
phase: 01-reorganize-and-bootstrap
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - ui/jukebox/NOTES.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03
  - REM-03

must_haves:
  truths:
    - "ui/jukebox/NOTES.md exists with all 4 required sections"
    - "Section 1 describes the task as assigned by the teacher"
    - "Section 2 explains the core concept (LazyVerticalGrid, MediaPlayer, data class)"
    - "Section 3 describes what was hard"
    - "Section 4 contains exactly one focused code snippet (5-15 lines)"
    - "The MediaPlayer overlap bug is documented as a known limitation with an explanation of why rapid taps trigger multiple streams"
    - "The file uses brief bullets (2-4 per section), no lengthy paragraphs"
  artifacts:
    - path: "ui/jukebox/NOTES.md"
      provides: "Retroactive learning notes for the jukebox app"
      contains: "## 1. Task Assigned"
  key_links:
    - from: "ui/jukebox/NOTES.md"
      to: "ui/jukebox/app/src/main/java/com/maxkeenti/jukebox/MainActivity.kt"
      via: "Key code snippet section"
      pattern: "```kotlin"
---

<objective>
Write the retroactive NOTES.md for the jukebox app at ui/jukebox/NOTES.md. This documents what the task was, what concept it demonstrates (LazyVerticalGrid being the primary lesson), what was hard, and documents the known MediaPlayer overlap bug.

Purpose: The jukebox app has no notes and has a known bug (MediaPlayer streams overlap on rapid taps). NOTES.md preserves the learning record and documents the limitation honestly rather than silently.
Output: ui/jukebox/NOTES.md with 4 sections, brief bullets, one focused code snippet, and MediaPlayer overlap documented as a known limitation.
</objective>

<execution_context>
@/Users/maximilianogonzalezcalzada/Source/Personal/android-development/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximilianogonzalezcalzada/Source/Personal/android-development/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-reorganize-and-bootstrap/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write NOTES.md for the jukebox app</name>
  <read_first>
    - ui/jukebox/app/src/main/java/com/maxkeenti/jukebox/MainActivity.kt (full source — LazyVerticalGrid, MediaPlayer, JukeboxItem data class)
  </read_first>
  <files>ui/jukebox/NOTES.md</files>
  <action>
    Create ui/jukebox/NOTES.md with exactly this structure. Follow D-01 (2-4 bullets per section), D-02 (one focused code snippet, 5-15 lines), and D-06 (document MediaPlayer overlap as a known limitation with explanation of why it happens — no code fix).

    The primary lesson for this app is LazyVerticalGrid (not MediaPlayer — the app is placed under ui/ for that reason).

    The file must contain these exact four section headings:
    - ## 1. Task Assigned
    - ## 2. Concept Covered
    - ## 3. What Was Hard
    - ## 4. Key Code Snippet

    Write the content as follows:

    ---

    # Jukebox App

    ## 1. Task Assigned

    - Build a 3-column sound board grid using Jetpack Compose
    - Each cell displays an image with a label; tapping the image plays a sound clip
    - Teacher provided Star Wars character assets (images + audio); goal was to learn `LazyVerticalGrid`

    ## 2. Concept Covered

    - `LazyVerticalGrid` with `GridCells.Fixed(3)` — lays out items in a fixed 3-column grid, rendering only visible cells
    - `data class JukeboxItem(label, imageRes, soundRes)` — groups related resource IDs into a single model object
    - `MediaPlayer.create(context, soundRes)` + `setOnCompletionListener { it.release() }` — creates a player per tap and releases it when playback ends
    - `painterResource(id = item.imageRes)` inside a `clickable` modifier to make images interactive

    ## 3. What Was Hard

    - Understanding `LazyVerticalGrid` vs `LazyColumn` — grid requires `columns` parameter specifying fixed or adaptive layout
    - Resource IDs (`R.drawable.*`, `R.raw.*`) must match the exact filename in the `res/` directory; typos cause runtime crashes, not compile errors
    - MediaPlayer lifecycle: creating a new instance per tap works but causes overlapping audio if the user taps quickly — see Known Limitation below

    **Known Limitation — MediaPlayer overlap on rapid taps:**
    Each tap creates a brand new `MediaPlayer` instance immediately, before the previous one has finished playing. Android has no tap-rate limiter by default. If the user taps an image three times quickly, three `MediaPlayer` instances start simultaneously, producing overlapping audio. The fix would be to track a shared `MediaPlayer` reference and call `stop()` + `release()` before creating the next one — but that requires `remember { }` state in Compose, which was not covered in this task.

    ## 4. Key Code Snippet

    The clickable image that plays a sound — the core interaction in the grid cell:

    ```kotlin
    Image(
        painter = painterResource(id = item.imageRes),
        contentDescription = item.label,
        modifier = Modifier
            .size(100.dp)
            .clickable {
                val mediaPlayer = MediaPlayer.create(context, item.soundRes)
                mediaPlayer.setOnCompletionListener { it.release() }
                mediaPlayer.start()
            }
    )
    ```

    ---

    Write the file with this exact content. Do not add additional sections. The Known Limitation is placed inside Section 3 (What Was Hard) because it is part of what was learned, not a separate section.
  </action>
  <verify>
    - Run: grep -c "^## " ui/jukebox/NOTES.md
    - Expected: 4 (exactly four section headings)
    - Run: grep "Known Limitation" ui/jukebox/NOTES.md
    - Expected: match found (MediaPlayer bug documented)
    - Run: grep '```kotlin' ui/jukebox/NOTES.md
    - Expected: match found (code block present)
    - Run: grep "LazyVerticalGrid" ui/jukebox/NOTES.md
    - Expected: match found (primary concept documented)
  </verify>
  <acceptance_criteria>
    - ui/jukebox/NOTES.md exists
    - grep "## 1. Task Assigned" ui/jukebox/NOTES.md returns a match
    - grep "## 2. Concept Covered" ui/jukebox/NOTES.md returns a match
    - grep "## 3. What Was Hard" ui/jukebox/NOTES.md returns a match
    - grep "## 4. Key Code Snippet" ui/jukebox/NOTES.md returns a match
    - grep "Known Limitation" ui/jukebox/NOTES.md returns a match (REM-03 satisfied)
    - grep "overlapping audio\|multiple streams\|overlap" ui/jukebox/NOTES.md returns a match (explains WHY)
    - grep '```kotlin' ui/jukebox/NOTES.md returns a match
    - grep -c "^## " ui/jukebox/NOTES.md returns 4 (no extra sections)
  </acceptance_criteria>
  <done>ui/jukebox/NOTES.md written with 4 sections, MediaPlayer overlap documented as a known limitation with explanation, and one focused Kotlin code snippet</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer → NOTES.md | Documentation file; no secrets, no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | NOTES.md content | accept | Personal learning notes; no PII, no credentials, no secrets |
| T-04-02 | Tampering | Code snippet accuracy | mitigate | Read MainActivity.kt before writing snippet; snippet must reflect actual clickable Image implementation |
</threat_model>

<verification>
After task completion:
- ui/jukebox/NOTES.md exists with 4 sections
- MediaPlayer overlap bug documented with explanation of why rapid taps cause multiple streams
- Code snippet reflects the actual clickable Image block from MainActivity.kt
- LazyVerticalGrid documented as the primary concept (not MediaPlayer)
</verification>

<success_criteria>
- DOCS-03: Retroactive NOTES.md written for the jukebox app
- DOCS-01 (partial): Template format applied — 4-section structure with brief bullets and one code snippet
- REM-03: MediaPlayer overlap documented in NOTES.md as a known limitation with explanation (per D-06)
</success_criteria>

<output>
After completion, create `.planning/phases/01-reorganize-and-bootstrap/01-04-SUMMARY.md`
</output>
