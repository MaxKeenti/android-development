---
phase: 01-reorganize-and-bootstrap
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - ui/news/NOTES.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - REM-04

must_haves:
  truths:
    - "ui/news/NOTES.md exists with all 4 required sections"
    - "Section 1 describes the task as assigned by the teacher"
    - "Section 2 explains the core concept (Compose card layout, composable parameters)"
    - "Section 3 describes what was hard (German text as starting point, composable parameter passing)"
    - "Section 4 contains exactly one focused code snippet (5-15 lines) demonstrating the core concept"
    - "The file uses brief bullets (2-4 per section), no lengthy paragraphs"
  artifacts:
    - path: "ui/news/NOTES.md"
      provides: "Retroactive learning notes for the news app"
      contains: "## 1. Task Assigned"
  key_links:
    - from: "ui/news/NOTES.md"
      to: "ui/news/app/src/main/java/com/maxkeenti/news/News.kt"
      via: "Key code snippet section"
      pattern: "```kotlin"
---

<objective>
Write the retroactive NOTES.md for the news app at ui/news/NOTES.md. This documents what the task was, what concept it demonstrates, what was hard, and includes the most important code snippet.

Purpose: The news app has no notes. Without NOTES.md, the code is an artifact with no context — the learning record is lost. This file establishes that record retroactively.
Output: ui/news/NOTES.md with 4 sections, brief bullets, one focused code snippet.
</objective>

<execution_context>
@/Users/maximilianogonzalezcalzada/Source/Personal/android-development/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximilianogonzalezcalzada/Source/Personal/android-development/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-reorganize-and-bootstrap/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write NOTES.md for the news app</name>
  <read_first>
    - ui/news/app/src/main/java/com/maxkeenti/news/News.kt (core composable — this is the key code to document)
    - ui/news/app/src/main/java/com/maxkeenti/news/MainActivity.kt (entry point with English placeholder strings, post-Plan-02)
  </read_first>
  <files>ui/news/NOTES.md</files>
  <action>
    Create ui/news/NOTES.md with exactly this structure. Follow D-01 (2-4 bullets per section) and D-02 (one focused code snippet, 5-15 lines, the most important demonstration of the concept).

    The file must contain these exact four section headings:
    - ## 1. Task Assigned
    - ## 2. Concept Covered
    - ## 3. What Was Hard
    - ## 4. Key Code Snippet

    Write the content as follows:

    ---

    # News App

    ## 1. Task Assigned

    - Build a news card UI in Jetpack Compose
    - Display a single news article with title, author, date, and content fields
    - Teacher provided the composable structure; goal was to learn how composables receive and display data

    ## 2. Concept Covered

    - Composable functions with named parameters: `News(title, author, date, content)`
    - `Card` with `RoundedCornerShape` and `CardDefaults.cardElevation` for Material 3 card styling
    - `Column` and `Row` for layout hierarchy — nesting rows inside a column for the header/body split
    - Passing `Modifier` as the last parameter with a default value: `modifier: Modifier = Modifier`

    ## 3. What Was Hard

    - Understanding when to use `Row` vs `Column` — the header needed a `Row` for full-width background, the body used nested columns for author/date and content
    - The original strings were in German (the teacher's example); they were replaced with English placeholders to make the code self-documenting
    - Getting the card to stretch full width required `Modifier.fillMaxWidth()` on the `Card`

    ## 4. Key Code Snippet

    The `News` composable — a card with a dark header row and a body with two columns:

    ```kotlin
    @Composable
    fun News(title: String, author: String, date: String, content: String) {
        Card(
            modifier = Modifier.fillMaxWidth().padding(16.dp),
            shape = RoundedCornerShape(4.dp),
            elevation = CardDefaults.cardElevation(4.dp)
        ) {
            Column {
                Row(
                    modifier = Modifier.fillMaxWidth().background(Color.Black).padding(12.dp)
                ) {
                    Text(text = title, color = Color.White)
                }
                Row {
                    Column { Text(text = author); Text(text = date) }
                    Column { Text(text = content) }
                }
            }
        }
    }
    ```

    ---

    Write the file with this exact content. Do not add additional sections, do not use lengthy paragraphs in the bullet sections.
  </action>
  <verify>
    - Run: grep -c "^## " ui/news/NOTES.md
    - Expected: 4 (exactly four section headings)
    - Run: grep "## 1. Task Assigned" ui/news/NOTES.md
    - Expected: match found
    - Run: grep "## 4. Key Code Snippet" ui/news/NOTES.md
    - Expected: match found
    - Run: grep '```kotlin' ui/news/NOTES.md
    - Expected: match found (code block present)
  </verify>
  <acceptance_criteria>
    - ui/news/NOTES.md exists
    - grep "## 1. Task Assigned" ui/news/NOTES.md returns a match
    - grep "## 2. Concept Covered" ui/news/NOTES.md returns a match
    - grep "## 3. What Was Hard" ui/news/NOTES.md returns a match
    - grep "## 4. Key Code Snippet" ui/news/NOTES.md returns a match
    - grep '```kotlin' ui/news/NOTES.md returns a match (code block exists)
    - grep -c "^## " ui/news/NOTES.md returns 4 (no extra sections)
    - grep "Article Title\|Author Name\|placeholder" ui/news/NOTES.md returns no German strings
  </acceptance_criteria>
  <done>ui/news/NOTES.md written with 4 sections, bullet format, and one focused Kotlin code snippet documenting the News composable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer → NOTES.md | Documentation file; no secrets, no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | NOTES.md content | accept | File is a learning note in a personal repo; no PII, no secrets, no credentials — no risk |
| T-03-02 | Tampering | Code snippet accuracy | mitigate | Read News.kt before writing snippet; snippet must match actual source in News.kt, not be invented |
</threat_model>

<verification>
After task completion:
- ui/news/NOTES.md exists with 4 sections
- No German text anywhere in the file
- Code snippet reflects actual News() composable from News.kt
- Each section uses bullets, not paragraphs
</verification>

<success_criteria>
- DOCS-02: Retroactive NOTES.md written for the news app
- DOCS-01 (partial): Template format applied — 4-section structure with brief bullets and one code snippet
- REM-04: German text context documented — notes acknowledge the strings were in German and explain the replacement
</success_criteria>

<output>
After completion, create `.planning/phases/01-reorganize-and-bootstrap/01-03-SUMMARY.md`
</output>
