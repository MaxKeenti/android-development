---
phase: 01-reorganize-and-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/news/
  - ui/jukebox/
autonomous: true
requirements:
  - REM-01
  - REM-02
  - STRUCT-01

must_haves:
  truths:
    - "Directory ui/news/ exists and contains a valid Android Studio Gradle project"
    - "Directory ui/jukebox/ exists and contains a valid Android Studio Gradle project"
    - "Original news/ and jukebox/ directories at root no longer exist"
    - "The two-level topic/task folder structure (ui/<task>) is established"
  artifacts:
    - path: "ui/news/app/src/main/java/com/maxkeenti/news/MainActivity.kt"
      provides: "news app source at new location"
    - path: "ui/news/app/build.gradle.kts"
      provides: "news build config at new location"
    - path: "ui/jukebox/app/src/main/java/com/maxkeenti/jukebox/MainActivity.kt"
      provides: "jukebox app source at new location"
    - path: "ui/jukebox/app/build.gradle.kts"
      provides: "jukebox build config at new location"
  key_links:
    - from: "ui/news/"
      to: "Gradle project root"
      via: "settings.gradle.kts at ui/news/settings.gradle.kts"
      pattern: "rootProject.name"
    - from: "ui/jukebox/"
      to: "Gradle project root"
      via: "settings.gradle.kts at ui/jukebox/settings.gradle.kts"
      pattern: "rootProject.name"
---

<objective>
Relocate both existing Android apps from the repository root into the correct topic/task folder structure. This establishes the two-level hierarchy (ui/<task>) that all future work will follow.

Purpose: Both apps currently sit flat at the root with no topic context. Moving them to ui/news/ and ui/jukebox/ creates the organizational structure that Phase 1 exists to establish.
Output: ui/news/ and ui/jukebox/ directories containing fully intact Gradle projects; root-level news/ and jukebox/ directories removed.
</objective>

<execution_context>
@/Users/maximilianogonzalezcalzada/Source/Personal/android-development/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximilianogonzalezcalzada/Source/Personal/android-development/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-reorganize-and-bootstrap/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ui/ topic directory and move news app</name>
  <read_first>
    - news/settings.gradle.kts (verify self-contained project structure before move)
    - news/app/build.gradle.kts (confirm no root-relative paths)
  </read_first>
  <files>ui/news/ (created from news/)</files>
  <action>
    Run the following shell commands in sequence:

    1. Create the ui/ topic directory:
       mkdir -p /Users/maximilianogonzalezcalzada/Source/Personal/android-development/ui

    2. Move the news app into it:
       mv /Users/maximilianogonzalezcalzada/Source/Personal/android-development/news /Users/maximilianogonzalezcalzada/Source/Personal/android-development/ui/news

    The move is safe because:
    - news/ is a self-contained Gradle project with no root-level references
    - No CI scripts or build files at the root reference the news/ path
    - Android Studio opens each project by pointing at its own folder; after the move, point Android Studio at ui/news/

    Do NOT rename any files inside the project or change any source paths — the move is purely at the filesystem level.
  </action>
  <verify>
    - Run: ls /Users/maximilianogonzalezcalzada/Source/Personal/android-development/ui/news/
    - Expected: settings.gradle.kts, build.gradle.kts, app/ directory visible
    - Run: ls /Users/maximilianogonzalezcalzada/Source/Personal/android-development/ | grep "^news$"
    - Expected: no output (news/ no longer at root)
  </verify>
  <acceptance_criteria>
    - ui/news/settings.gradle.kts exists
    - ui/news/app/build.gradle.kts exists
    - ui/news/app/src/main/java/com/maxkeenti/news/MainActivity.kt exists
    - Root-level news/ directory does NOT exist (ls at repo root returns no "news" entry)
  </acceptance_criteria>
  <done>news Gradle project accessible at ui/news/ with all source files intact; root news/ removed</done>
</task>

<task type="auto">
  <name>Task 2: Move jukebox app into ui/ topic directory</name>
  <read_first>
    - jukebox/settings.gradle.kts (verify self-contained project structure before move)
    - jukebox/app/build.gradle.kts (confirm no root-relative paths)
  </read_first>
  <files>ui/jukebox/ (created from jukebox/)</files>
  <action>
    Run the following shell command:

    mv /Users/maximilianogonzalezcalzada/Source/Personal/android-development/jukebox /Users/maximilianogonzalezcalzada/Source/Personal/android-development/ui/jukebox

    The move is safe because:
    - jukebox/ is a self-contained Gradle project with no root-level references
    - No CI scripts or build files at the root reference the jukebox/ path
    - Android Studio opens each project by pointing at its own folder; after the move, point Android Studio at ui/jukebox/

    Do NOT rename any files inside the project or change any source paths — the move is purely at the filesystem level.
  </action>
  <verify>
    - Run: ls /Users/maximilianogonzalezcalzada/Source/Personal/android-development/ui/jukebox/
    - Expected: settings.gradle.kts, build.gradle.kts, app/ directory visible
    - Run: ls /Users/maximilianogonzalezcalzada/Source/Personal/android-development/ | grep "^jukebox$"
    - Expected: no output (jukebox/ no longer at root)
  </verify>
  <acceptance_criteria>
    - ui/jukebox/settings.gradle.kts exists
    - ui/jukebox/app/build.gradle.kts exists
    - ui/jukebox/app/src/main/java/com/maxkeenti/jukebox/MainActivity.kt exists
    - Root-level jukebox/ directory does NOT exist (ls at repo root returns no "jukebox" entry)
  </acceptance_criteria>
  <done>jukebox Gradle project accessible at ui/jukebox/ with all source files intact; root jukebox/ removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell → filesystem | mv commands act on tracked Git files; accidental data loss if wrong path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | mv command paths | mitigate | Use absolute paths only; verify source exists before move; run ls verify step after each move |
| T-01-02 | Denial of Service | Git history loss | accept | Git tracks renames automatically when similarity threshold is met; low risk for self-contained directory moves; no secrets in moved files |
</threat_model>

<verification>
After both tasks complete:
- ls ui/ shows: news/ jukebox/
- ls at repo root shows no news/ or jukebox/ entries
- ui/news/app/src/main/java/com/maxkeenti/news/MainActivity.kt is readable
- ui/jukebox/app/src/main/java/com/maxkeenti/jukebox/MainActivity.kt is readable
</verification>

<success_criteria>
- Both apps relocated to ui/<task>/ structure (STRUCT-01 two-level hierarchy established)
- REM-01: news at ui/news/
- REM-02: jukebox at ui/jukebox/
- No files lost — all source, resources, and build config intact at new paths
</success_criteria>

<output>
After completion, create `.planning/phases/01-reorganize-and-bootstrap/01-01-SUMMARY.md`
</output>
