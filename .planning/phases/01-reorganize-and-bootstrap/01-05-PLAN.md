---
phase: 01-reorganize-and-bootstrap
plan: 05
type: execute
wave: 4
depends_on:
  - 01-03
  - 01-04
files_modified:
  - README.md
  - ui/README.md
autonomous: true
requirements:
  - STRUCT-02
  - STRUCT-03
  - CONV-01
  - CONV-02
  - CONV-03

must_haves:
  truths:
    - "Root README.md exists with a brief intro paragraph and a topic/task navigation index"
    - "Root README.md contains the NOTES.md template inline (4 sections defined)"
    - "Root README.md contains the topic decision gate (rule for which topic folder a task belongs in)"
    - "Root README.md contains the package naming convention with the legacy exception documented"
    - "ui/README.md exists listing both tasks (news and jukebox) with brief descriptions"
    - "A new reader can navigate from README.md to any app in the repo"
  artifacts:
    - path: "README.md"
      provides: "Root navigable index with inline conventions"
      contains: "## NOTES.md Template"
    - path: "ui/README.md"
      provides: "Topic-level index for the ui/ topic"
      contains: "news"
  key_links:
    - from: "README.md"
      to: "ui/news/"
      via: "navigation index link"
      pattern: "ui/news"
    - from: "README.md"
      to: "ui/jukebox/"
      via: "navigation index link"
      pattern: "ui/jukebox"
    - from: "ui/README.md"
      to: "ui/news/NOTES.md"
      via: "task listing link"
      pattern: "news"
---

<objective>
Create the root README.md and the ui/README.md. The root README serves as the navigable entry point for the entire repo, and embeds all three conventions (NOTES.md template, topic decision gate, package naming policy) inline per D-05 and D-08. The ui/README.md serves as the topic-level index since the ui/ topic now has 2 tasks.

Purpose: Without README files, the repo is opaque to any future reader (including the owner returning after time away). These files make the structure self-evident and record all three conventions in one place.
Output: README.md at repo root; ui/README.md at topic level. All conventions embedded inline in root README.
</objective>

<execution_context>
@/Users/maximilianogonzalezcalzada/Source/Personal/android-development/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maximilianogonzalezcalzada/Source/Personal/android-development/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-reorganize-and-bootstrap/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root README.md with navigation index and inline conventions</name>
  <read_first>
    - ui/news/NOTES.md (exists after Plan 03 — confirms news task is documented)
    - ui/jukebox/NOTES.md (exists after Plan 04 — confirms jukebox task is documented)
    - .planning/REQUIREMENTS.md (CONV-01, CONV-02, CONV-03 requirements — exact acceptance criteria)
    - .planning/phases/01-reorganize-and-bootstrap/01-CONTEXT.md (D-04, D-05, D-08 decisions)
  </read_first>
  <files>README.md</files>
  <action>
    Create README.md at the repository root. Follow D-04 (brief intro paragraph first, then navigation index), D-05 (NOTES.md template embedded inline, not a separate file), and D-08 (CONV-02 and CONV-03 also inline in root README).

    The file structure must be:
    1. H1 title
    2. Intro paragraph (Claude's discretion per D-04)
    3. ## Topics navigation section with task index
    4. ## NOTES.md Template section (satisfies CONV-01)
    5. ## Topic Decision Gate section (satisfies CONV-02)
    6. ## Package Naming Convention section (satisfies CONV-03)

    Write the file with this content:

    ---

    # Android Development Learning Journey

    A personal reference repo for Android development tasks assigned by my teacher. Each task lives in a topic folder alongside working code and notes explaining what I learned. This is a living record of learning in progress — not a course, not a portfolio.

    ## Topics

    ### ui/ — User Interface

    | Task | What It Demonstrates |
    |------|---------------------|
    | [news](ui/news/) | Jetpack Compose `Card` layout with composable parameters |
    | [jukebox](ui/jukebox/) | `LazyVerticalGrid` with `MediaPlayer` tap-to-play audio |

    See [ui/README.md](ui/README.md) for the topic overview.

    ---

    ## NOTES.md Template

    Every task folder contains a `NOTES.md` using this template. Copy it when starting a new task.

    ```markdown
    # [App Name]

    ## 1. Task Assigned

    - [What the teacher asked you to build — 2-4 bullets]

    ## 2. Concept Covered

    - [The primary Android/Compose concept this task teaches — 2-4 bullets]

    ## 3. What Was Hard

    - [The friction points, gotchas, and surprises — 2-4 bullets]

    ## 4. Key Code Snippet

    [One focused block (5-15 lines) showing the most important part of the implementation]

    ```kotlin
    // paste the key code here
    ```
    ```

    **Rules:**
    - 2-4 bullet points per section — no lengthy paragraphs
    - Section 4 contains exactly one code snippet — the most important 5-15 lines
    - No additional sections without a good reason

    ---

    ## Topic Decision Gate

    When a new task arrives, pick its topic folder using this rule:

    **Ask: "What is the primary concept this task teaches?"**

    | If the task primarily teaches... | Topic folder |
    |----------------------------------|-------------|
    | UI layout, Compose components, navigation, theming | `ui/` |
    | Audio, video, camera, media playback | `media/` |
    | Data persistence, local database, file storage | `data/` |
    | Network calls, REST APIs, remote data | `network/` |
    | Background work, services, scheduling | `background/` |
    | Sensors, location, hardware APIs | `sensors/` |

    **If a task spans multiple concepts:** Use the concept the teacher is *introducing for the first time*. For example, the Jukebox app uses `MediaPlayer`, but the teacher's goal was `LazyVerticalGrid` — so it lives in `ui/`, not `media/`.

    **If unsure:** Ask the teacher what concept they want you to focus on.

    **Create a new topic folder** when a task does not fit any existing topic. Add it to this table.

    ---

    ## Package Naming Convention

    **Convention going forward:** New apps use the package name `com.maxkeenti.<topic>.<taskname>` — for example, a future `ui/calculator/` app would use `com.maxkeenti.ui.calculator`.

    **Legacy exception:** The two existing apps (`news`, `jukebox`) use flat package names (`com.maxkeenti.news`, `com.maxkeenti.jukebox`) without a topic segment. These are not being refactored — the churn is not worth the benefit for a private learning repo. Document this exception here so it is intentional, not an oversight.

    ---

    Write the file with this exact content. The intro paragraph wording is Claude's discretion (D-04) — keep it brief (2-4 sentences), personal, and clear about purpose. The table format for the task index is Claude's discretion (D-05 scoping).
  </action>
  <verify>
    - Run: grep "## NOTES.md Template" README.md
    - Expected: match found
    - Run: grep "## Topic Decision Gate" README.md
    - Expected: match found
    - Run: grep "## Package Naming Convention" README.md
    - Expected: match found
    - Run: grep "ui/news" README.md
    - Expected: match found
    - Run: grep "ui/jukebox" README.md
    - Expected: match found
    - Run: grep '```markdown' README.md
    - Expected: match found (template is embedded as a code block)
  </verify>
  <acceptance_criteria>
    - README.md exists at repo root
    - grep "## NOTES.md Template" README.md returns a match (CONV-01 satisfied)
    - grep "## Topic Decision Gate" README.md returns a match (CONV-02 satisfied)
    - grep "## Package Naming Convention" README.md returns a match (CONV-03 satisfied)
    - grep "ui/news" README.md returns a match (news task indexed)
    - grep "ui/jukebox" README.md returns a match (jukebox task indexed)
    - grep "ui/README.md" README.md returns a match (link to topic README present)
    - grep "com.maxkeenti" README.md returns a match (package naming example present)
    - grep "Legacy exception\|legacy exception" README.md returns a match (CONV-03 legacy documented)
    - grep '```markdown' README.md returns a match (template embedded as code block)
  </acceptance_criteria>
  <done>Root README.md created with intro, task index, and all three conventions inline (NOTES.md template, topic gate, package naming with legacy exception)</done>
</task>

<task type="auto">
  <name>Task 2: Create ui/README.md as topic-level index</name>
  <read_first>
    - README.md (just created in Task 1 — confirms navigation link target)
    - ui/news/NOTES.md (exists — confirm task is documented before linking)
    - ui/jukebox/NOTES.md (exists — confirm task is documented before linking)
  </read_first>
  <files>ui/README.md</files>
  <action>
    Create ui/README.md as the topic-level index for the `ui/` topic. This file exists because the topic has 2 tasks (STRUCT-03 requirement: create a topic README when a topic reaches 2+ tasks).

    Write the file with this content:

    ---

    # ui/ — User Interface Tasks

    Tasks in this topic teach Jetpack Compose UI concepts: layout, components, theming, and user interaction.

    ## Tasks

    | Task | Primary Concept | Notes |
    |------|----------------|-------|
    | [news](news/) | `Card` layout, composable parameters | [NOTES.md](news/NOTES.md) |
    | [jukebox](jukebox/) | `LazyVerticalGrid`, tap-to-play with `MediaPlayer` | [NOTES.md](jukebox/NOTES.md) |

    ## When to Add to ui/

    A new task belongs in `ui/` when the primary concept the teacher is introducing is a UI layout, Compose component, navigation pattern, or theming API. See the [Topic Decision Gate](../README.md#topic-decision-gate) in the root README for the full rule.

    ---

    Write the file with this exact content.
  </action>
  <verify>
    - Run: grep "news" ui/README.md
    - Expected: match found
    - Run: grep "jukebox" ui/README.md
    - Expected: match found
    - Run: grep "NOTES.md" ui/README.md
    - Expected: at least 2 matches (links to both NOTES.md files)
  </verify>
  <acceptance_criteria>
    - ui/README.md exists
    - grep "news" ui/README.md returns a match
    - grep "jukebox" ui/README.md returns a match
    - grep "news/NOTES.md\|news.*NOTES" ui/README.md returns a match (link to news notes)
    - grep "jukebox/NOTES.md\|jukebox.*NOTES" ui/README.md returns a match (link to jukebox notes)
    - grep "Topic Decision Gate\|topic-decision-gate" ui/README.md returns a match (back-link to root convention)
  </acceptance_criteria>
  <done>ui/README.md created as topic-level index listing both tasks with links to their NOTES.md files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| writer → README files | Markdown documentation; no secrets, no executable code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | README.md content | accept | Personal learning notes repo; no PII, no secrets, no credentials — content is intentionally public-facing as learning record |
| T-05-02 | Tampering | Broken relative links in README | mitigate | Verify ui/news/ and ui/jukebox/ exist before writing links; grep for link targets after writing |
</threat_model>

<verification>
After both tasks:
- README.md at repo root contains navigation index, NOTES.md template, topic gate, and package naming convention
- ui/README.md lists both tasks with links to their NOTES.md files
- grep "## NOTES.md Template" README.md returns match
- grep "## Topic Decision Gate" README.md returns match
- grep "## Package Naming Convention" README.md returns match
</verification>

<success_criteria>
- STRUCT-02: Root README.md exists as navigable index listing both tasks under ui/
- STRUCT-03: ui/README.md created because ui/ topic has 2 tasks
- CONV-01: NOTES.md template embedded inline in root README
- CONV-02: Topic decision gate documented inline in root README
- CONV-03: Package naming convention with legacy exception documented inline in root README
</success_criteria>

<output>
After completion, create `.planning/phases/01-reorganize-and-bootstrap/01-05-SUMMARY.md`
</output>
